package Aug21.Program2;
public class Address {
    private String street;
    private String City;
    public Address(String street, String city) {
        this.street = street; City = city;}
    public String getStreet() {return street;}
    public String getCity() {return City;}}


/*
1. Create an `Address` class:
- Declare private variables `street` and `city` to store the street and city of the address.
- Create a constructor `Address(street, city)` to initialize the `street` and `city` variables.
- Create methods `getStreet()` and `getCity()` to access the street and city values.
 */